<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.travelproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.TravelProject">
        <activity
            android:name=".ListActivity"
            android:exported="false" />
        <activity
            android:name=".TripMapActivity"
            android:exported="false"
            android:label="@string/title_activity_trip_map"></activity>
        <activity
            android:name=".NewTripActivity"
            android:exported="false" />
        <activity
            android:name=".MyTripsActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TravelProject.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".log.LogActivity"
            android:exported="false"></activity>
    </application>

</manifest>